{% extends 'base.html.twig' %}

{% block title %}{{ category.name }}{% endblock %}

{% block body %}
<h1>{{ category.name }}</h1>
<div class="row">
    <div class="col-12">
        <h2>Description :</h2>
        <p>{{ category.description }}</p>
    </div>
</div>
{% endblock %}
